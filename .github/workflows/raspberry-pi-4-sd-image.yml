# This file is automatically generated from Nix configuration. Do not edit directly.

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
            extra-substituters = https://cache.janusz-bit.com/nixos-builds
            extra-trusted-public-keys = nixos-builds:FdfmW2lSPWomDoWn5dNZv5ZJa+i5nL8niWqk/RKVWRc=
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Setup Attic cache
        uses: ryanccn/attic-action@v0
        with:
          cache: nixos-builds
          endpoint: https://cache.janusz-bit.com/
          token: ${{ secrets.ATTIC_TOKEN }}
      - name: raspberry-pi-4-sd-image
        run: nix build .#packages.aarch64-linux.raspberry-pi-4-sd-image --show-trace --accept-flake-config
name: raspberry-pi-4-sd-image
"on":
  pull_request:
    branches:
      - master
  push:
    tags:
      - v*
  workflow_dispatch: {}
run-name: Build raspberry-pi-4-sd-image by @${{ github.actor }}
