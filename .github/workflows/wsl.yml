# This file is automatically generated from Nix configuration. Do not edit directly.

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          extra_nix_config: |
            experimental-features = nix-command flakes
            access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
            extra-substituters = https://cache.janusz-bit.com/nixos-builds
            extra-trusted-public-keys = nixos-builds:g7DtqKioAfGeX76wt4lF9gzrpCj1ZCs8HGThHGwL5iA=
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Setup Attic cache
        uses: ryanccn/attic-action@v0
        with:
          cache: nixos-builds
          endpoint: https://cache.janusz-bit.com/
          token: ${{ secrets.ATTIC_TOKEN }}
      - name: wsl
        run: nix build .#nixosConfigurations.wsl.config.system.build.toplevel --show-trace --accept-flake-config
name: wsl
"on":
  pull_request:
    branches:
      - master
  push:
    tags:
      - v*
  workflow_dispatch: {}
run-name: Build wsl by @${{ github.actor }}
